# 📖 PLAYBOOK: Как получить данные о чате в teleeng_school

## 🎯 Когда использовать этот плейбук

Когда пользователь спрашивает про конкретный чат по его ID (например, `-1003004153948`):
- "Что это за чат?"
- "Какой там пресет/курс?"
- "Какие настройки у этого чата?"
- "Расскажи про чат с ID ..."

---

## 📋 ЧТО ХОЧЕТ УЗНАТЬ ПОЛЬЗОВАТЕЛЬ

Пользователь хочет получить **полную информацию о конкретном чате**:

1. **Основные атрибуты чата**: название, тип, ID, статус
2. **Пресет**: какой пресет привязан, его настройки
3. **Курсы**: какие программы обучения активны, на каком уроке
4. **Цены**: стоимость подписки, триалов
5. **Настройки**: лимиты уроков, проверки, ИИ, лобби
6. **Структура**: какие треды созданы (лобби, участники, ИИ)

---

## 🛠️ ШАГ ЗА ШАГОМ: Как получить данные

### 📍 ШАГ 1: Понять, что данные в БД на сервере

❌ **НЕ ИЩИТЕ** chat ID в локальном коде — его там нет!  
✅ **Данные хранятся** в базе PostgreSQL на сервере Hetzner

**Путь к серверу:** `root@65.21.49.91`  
**База данных:** `school` на `10.0.0.3:5432`  
**Логин:** `teleeng` | **Пароль:** `S65EcfVRXYs`

---

### 📍 ШАГ 2: Установить SSH туннель

```bash
ssh -f -N -L 5433:10.0.0.3:5432 root@65.21.49.91
```

**Опции:**
- `-f` — фоновый режим
- `-N` — не выполнять команды
- `-L 5433:10.0.0.3:5432` — пробросить порт PostgreSQL

**Проверка подключения:**
```bash
sleep 2  # Подождать установки туннеля
```

---

### 📍 ШАГ 3: Получить данные о чате из таблицы `clubs`

```bash
PGPASSWORD='S65EcfVRXYs' psql -h localhost -p 5433 -U teleeng -d school -c "
SELECT 
    c.id, 
    c.name, 
    c.club_chat_id, 
    c.club_type, 
    c.code, 
    c.price_rub, 
    c.price_eur, 
    c.trial_days_amt, 
    c.start_at, 
    c.is_forum, 
    c.debug, 
    c.lesson_limit_per_day, 
    c.checking_type, 
    c.lobby_thread_id, 
    c.initial_thread_id, 
    c.members_thread_id, 
    c.preset_id 
FROM clubs c 
WHERE c.club_chat_id = -1003004153948;
"
```

**Что получим:**
- ID клуба, название
- Тип клуба (individual/school/conv_club)
- Цены (RUB/EUR), триал
- Треды (лобби, ИИ, участники)
- **preset_id** — для следующего запроса

---

### 📍 ШАГ 4: Получить данные о пресете из таблицы `core_preset`

⚠️ **ВНИМАНИЕ:** Таблица содержит МНОГО полей, поэтому используем `SELECT *`

```bash
PGPASSWORD='S65EcfVRXYs' psql -h localhost -p 5433 -U teleeng -d school -c "
SELECT * 
FROM core_preset 
WHERE id = 83;
"
```

**Ключевые поля пресета:**
- `code` — код пресета (например, `individualyearnewai`)
- `club_basename` — базовое название (например, "Английский")
- `link_type` — тип (assistant/individual_education/group_education)
- `base_gpt_model` — модель GPT
- `checking_enabled` — проверка преподами
- `assistant_enabled` — ИИ ассистент
- `lobby_enabled` — лобби
- `lesson_limit_per_day` — лимит уроков
- `education_start_at` — дата начала обучения
- `welcome_message` — приветственное сообщение
- `content_config` — JSON конфигурация переключения программ

---

### 📍 ШАГ 5: Получить активные курсы из таблицы `program_chats`

```bash
PGPASSWORD='S65EcfVRXYs' psql -h localhost -p 5433 -U teleeng -d school -c "
SELECT 
    pc.*, 
    p.name as program_name 
FROM program_chats pc 
JOIN programs p ON pc.program_id = p.id 
WHERE pc.chat_id = -1003004153948;
"
```

**Что получим:**
- Список всех программ (курсов) чата
- Текущий урок (`current_class`)
- Статус активности (`is_active`)
- ID треда (`thread_id`)
- Расписание (`days_schedule`)
- Даты старта/обновления

---

### 📍 ШАГ 6: Закрыть SSH туннель

```bash
pkill -f "ssh.*5433:10.0.0.3:5432"
```

---

## 📊 СТРУКТУРА БД: Важные таблицы

### Таблица `clubs` (основная):
- **club_chat_id** — ID Telegram чата
- **name** — название клуба
- **club_type** — тип (individual/school/conv_club/assistant/program)
- **preset_id** — связь с пресетом
- **price_rub** / **price_eur** — цены
- **trial_days_amt** — дни триала
- **lesson_limit_per_day** — лимит уроков
- **checking_type** — тип проверки (homework/comment)
- **lobby_thread_id** — ID треда лобби
- **initial_thread_id** — ID треда ИИ
- **members_thread_id** — ID треда участников
- **is_forum** — форум или нет
- **debug** — debug режим

### Таблица `core_preset`:
- **id** — ID пресета
- **code** — уникальный код
- **club_basename** — базовое название
- **link_type** — тип ссылки
- **base_gpt_model** — модель GPT
- **checking_enabled** — проверка преподами
- **assistant_enabled** — ИИ включен
- **lobby_enabled** — лобби включено
- **profiles_enabled** — профили включены
- **education_start_at** — дата начала обучения
- **lesson_limit_per_day** — лимит уроков
- **welcome_message** — приветствие
- **content_config** — JSON с настройками переключения программ
- **source** / **marketing_source** — источники трафика

### Таблица `program_chats`:
- **id** — ID программы чата
- **program_id** — ID программы обучения
- **chat_id** — ID чата
- **thread_id** — ID треда (может быть NULL = лобби)
- **is_active** — активна ли программа
- **current_class** — текущий урок
- **days_schedule** — расписание (например, "1,2,3,4,5" = пн-пт)
- **start_at** — дата старта
- **created_at** / **updated_at** — даты создания/обновления

### Таблица `programs`:
- **id** — ID программы
- **name** — название курса

---

## 🎨 КАК ПРЕДСТАВИТЬ РЕЗУЛЬТАТ

Оформите результат в виде **структурированного отчёта**:

### 1️⃣ Основные атрибуты
Таблица с ID, названием, типом, ценами, статусом

### 2️⃣ Пресет
Таблица с кодом пресета, настройками, источниками

### 3️⃣ Активные курсы
Для каждого курса:
- Название программы
- Статус (активна/неактивна)
- Текущий урок
- Thread ID
- Расписание

### 4️⃣ Структура тредов
Список тредов (лобби, ИИ, участники) с ID

### 5️⃣ Ключевые особенности
Краткий список особенностей пресета:
- Проверка преподами вкл/выкл
- ИИ ассистент вкл/выкл
- Автоматическое переключение программ
- Кастомные сообщения

### 6️⃣ Краткий вывод
Резюме: тип чата, активный курс, стоимость, ключевые фичи, даты активности

---

## ⚠️ ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### Ошибка: `relation "core_club" does not exist`
❌ **Проблема:** Неправильное имя таблицы  
✅ **Решение:** Используйте `clubs` (без префикса `core_`)

### Ошибка: `relation "presets" does not exist`
❌ **Проблема:** Неправильное имя таблицы  
✅ **Решение:** Используйте `core_preset` (С префиксом!)

### Ошибка: `relation "core_programchat" does not exist`
❌ **Проблема:** Неправильное имя таблицы  
✅ **Решение:** Используйте `program_chats` (множественное число, без префикса)

### Ошибка: `column p.name does not exist`
❌ **Проблема:** Колонка отсутствует в таблице  
✅ **Решение:** Сначала выполните `SELECT *` чтобы узнать структуру

### Проверка имен таблиц:
```bash
PGPASSWORD='S65EcfVRXYs' psql -h localhost -p 5433 -U teleeng -d school -c "\dt" | grep -i <keyword>
```

---

## 🔗 СВЯЗАННЫЕ ДОКУМЕНТЫ

- **HETZNER_INFRASTRUCTURE.md** — общая информация о сервере и базах данных
- **Как различить платежи с лендинга от подписочных платежей.md** — аналитика платежей

---

## 📝 ПРИМЕЧАНИЯ

1. **SSH туннель** может занять 2-3 секунды для установки — добавьте `sleep 2`
2. **Таблица `core_preset`** имеет ~70+ полей — используйте `SELECT *` для первого запроса
3. **JSON поля** (например, `content_config`) содержат сложные настройки переключения программ
4. **Thread ID** может быть `NULL` — это означает, что контент идет в лобби
5. **Расписание** хранится как строка `"1,2,3,4,5"` где числа = дни недели (1=пн, 5=пт)
6. **Цены** хранятся в копейках/центах — делите на 100 для отображения
7. **Даты** в формате PostgreSQL timestamp with timezone

---

## ✅ ЧЕКЛИСТ ПЕРЕД ОТВЕТОМ

- [ ] SSH туннель установлен
- [ ] Получены данные из `clubs`
- [ ] Получены данные из `core_preset`
- [ ] Получены данные из `program_chats` + `programs`
- [ ] SSH туннель закрыт
- [ ] Результат оформлен в структурированный отчёт
- [ ] Добавлен краткий вывод

---

**Последнее обновление:** 29 октября 2025  
**Автор:** Cursor AI Assistant (на основе запроса пользователя)

